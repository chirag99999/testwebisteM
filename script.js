const qs=s=>document.querySelector(s), qsa=s=>[...document.querySelectorAll(s)];
const dot=qs('.cursor-dot'),ring=qs('.cursor-ring');document.addEventListener('mousemove',e=>{if(dot){dot.style.left=e.clientX+'px';dot.style.top=e.clientY+'px'}if(ring){ring.animate({left:e.clientX+'px',top:e.clientY+'px'},{duration:280,fill:'forwards'})}});
qsa('a,button,.cap-card,.case-card,.play-chip').forEach(el=>{el.addEventListener('mouseenter',()=>{ring&&(ring.style.width='58px',ring.style.height='58px',ring.style.background='#ef233c18')});el.addEventListener('mouseleave',()=>{ring&&(ring.style.width='36px',ring.style.height='36px',ring.style.background='transparent')})});
qsa('.magnetic').forEach(el=>el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.16}px,${(e.clientY-r.top-r.height/2)*.16}px)`}));qsa('.magnetic').forEach(el=>el.addEventListener('mouseleave',()=>el.style.transform=''));
const menu=qs('.menu-toggle'),mobile=qs('.mobile-menu');menu?.addEventListener('click',()=>mobile.classList.toggle('open'));qsa('.mobile-menu a').forEach(a=>a.addEventListener('click',()=>mobile.classList.remove('open')));
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});qsa('.reveal').forEach(e=>observer.observe(e));
qsa('.cap-card').forEach((card,i)=>{card.style.transitionDelay=(i%3)*70+'ms'});
qsa('.filter-bar button').forEach(btn=>btn.onclick=()=>{qsa('.filter-bar button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;qsa('.case-card').forEach(c=>c.classList.toggle('hide',f!=='all'&&c.dataset.type!==f))});
qsa('.accordion-item').forEach(btn=>btn.onclick=()=>{const panel=btn.nextElementSibling;qsa('.accordion-item').forEach(b=>b.classList.remove('active'));qsa('.accordion-panel').forEach(p=>p.classList.remove('open'));btn.classList.add('active');panel.classList.add('open')});
let quoteIndex=0;const quotes=qsa('.quote');function showQuote(i){quoteIndex=(i+quotes.length)%quotes.length;quotes.forEach((q,j)=>q.classList.toggle('active',j===quoteIndex))}qs('.quote-arrow.next')?.addEventListener('click',()=>showQuote(quoteIndex+1));qs('.quote-arrow.prev')?.addEventListener('click',()=>showQuote(quoteIndex-1));setInterval(()=>showQuote(quoteIndex+1),7000);
const counters=qsa('[data-count]');const countObs=new IntersectionObserver(es=>es.forEach(e=>{if(!e.isIntersecting||e.target.dataset.done)return;e.target.dataset.done='1';const end=+e.target.dataset.count;let n=0;const step=Math.max(1,Math.ceil(end/45));const t=setInterval(()=>{n=Math.min(end,n+step);e.target.textContent=n+(end>100?'+':'');if(n>=end)clearInterval(t)},25)}),{threshold:.7});counters.forEach(c=>countObs.observe(c));
const stage=qs('.hero-stage');stage?.addEventListener('mousemove',e=>{const r=stage.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;qs('.hero-photo-card').style.transform=`rotate(${x*4}deg) rotateX(${-y*3}deg) translate(${x*8}px,${y*8}px)`});stage?.addEventListener('mouseleave',()=>qs('.hero-photo-card').style.transform='rotate(2deg)');
qs('#briefForm')?.addEventListener('submit',e=>{e.preventDefault();const b=e.currentTarget.querySelector('button'),m=qs('.form-message');b.textContent='BRIEF RECEIVED ✓';b.disabled=true;m.textContent='Thanks — we’ll get back to you shortly.';e.currentTarget.reset()});
let last=0;window.addEventListener('scroll',()=>{const h=qs('.site-header');if(window.scrollY>80)h.classList.add('scrolled');else h.classList.remove('scrolled');last=window.scrollY},{passive:true});