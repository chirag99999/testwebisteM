document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');let f=b.dataset.filter;document.querySelectorAll('.case').forEach(c=>c.classList.toggle('hide',f!=='all'&&c.dataset.type!==f))});document.querySelectorAll('.accordion button').forEach((b,i)=>b.onclick=()=>{document.querySelectorAll('.accordion p').forEach((p,j)=>p.classList.toggle('show',i===j));document.querySelectorAll('.accordion button').forEach((x,j)=>x.classList.toggle('open',i===j))});document.querySelector('form').onsubmit=e=>{e.preventDefault();let b=e.target.querySelector('button');b.textContent='BRIEF RECEIVED ✓';b.disabled=true};